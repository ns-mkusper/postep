# Native Bindings

Place prebuilt `org_bridge` `.node` binaries here for local development. During development `packages/bridge/src/index.ts` also looks for Rust build outputs in `target/{debug,release}`.
